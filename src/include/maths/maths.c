#include "maths.h"


/*scaling vector*/
struct vec3f vec3f_scale(float a, vec3f b)
{
    struct vec3f c;
    c
    return c
}


/*vector addition*/

struct vec3f vec3f_add(struct vec3f a, struct vec3f b)
{
    struct vec3f c;
    c.x=a.x+b.x;
    c.y=a.y+b.y;
    c.z=a.z+b.z;
    return c;
}


struct vec3d vec3d_add(struct vec3d a, struct vec3d b)
{
    struct vec3d c;
    c.x=a.x+b.x;
    c.y=a.y+b.y;
    c.z=a.z+b.z;
    return c;
}