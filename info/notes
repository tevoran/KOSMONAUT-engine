This is a place for notes for improving KOSMONAUT:

-adding the feature the let the library use his owns paths for the shaders
-checking for skybox in new_frame(), engine expects a skybox
-renderer expects a window to be drawn other wise there is a crash